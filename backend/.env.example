DATABASE_URL=
GEMINI_API_KEY=
GROQ_API_KEY=
CHROMA_HOST=
CHROMA_PORT=
# Ollama fallback tier. localhost for local (non-Docker) dev. The containerized
# backend overrides this to http://host.docker.internal:11434 in docker-compose.yml.
OLLAMA_HOST=http://localhost:11434
