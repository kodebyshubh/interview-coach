# AI Interview Coach — build tracker

- [x] 2026-05-20 — Prompt 1 complete: scaffolded monorepo, started docker-compose services, and confirmed `GET /health` returns 200 OK.
- [x] 2026-05-20 — Installed Prisma ORM, added `.env.local`, configured `prisma/schema.prisma`, and installed Supabase agent skills.
- [x] 2026-05-20 — Standardized backend env vars to `backend/.env` (backed up old Prisma .env) so Prisma loads them by default.
- [x] 2026-05-20 — Prompt 2 complete: created SQLAlchemy models (`sessions`, `questions`, `answers`), `init_db` startup hook, and `/health/db` endpoint.
- [x] 2026-05-20 — Prompt 3 complete: added ChromaDB HTTP RAG embed/retrieve helpers and `/upload` endpoint; verified chunk counts > 0 and metadata filtering by `session_id`.
- [x] 2026-05-20 — Prompt 4 complete: `/upload` now accepts PDF multipart form-data, extracts text via PyPDF2, generates session_id server-side, and embeds resume/JD into Chroma.
- [x] 2026-05-20 — Prompt 5 complete: added `/debug/retrieve` + `/debug/session/{session_id}`; retrieved 3+3 chunks and they clearly contained the query term (e.g., “FastAPI … SQLAlchemy … Postgres … ChromaDB …”).
