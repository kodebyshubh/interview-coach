# AI Interview Coach — build tracker

- [x] 2026-05-20 — Prompt 1 complete: scaffolded monorepo, started docker-compose services, and confirmed `GET /health` returns 200 OK.
- [x] 2026-05-20 — Installed Prisma ORM, added `.env.local`, configured `prisma/schema.prisma`, and installed Supabase agent skills.
- [x] 2026-05-20 — Standardized backend env vars to `backend/.env` (backed up old Prisma .env) so Prisma loads them by default.
- [x] 2026-05-20 — Prompt 2 complete: created SQLAlchemy models (`sessions`, `questions`, `answers`), `init_db` startup hook, and `/health/db` endpoint.
- [x] 2026-05-20 — Prompt 3 complete: added ChromaDB HTTP RAG embed/retrieve helpers and `/upload` endpoint; verified chunk counts > 0 and metadata filtering by `session_id`.
- [x] 2026-05-20 — Prompt 4 complete: `/upload` now accepts PDF multipart form-data, extracts text via PyPDF2, generates session_id server-side, and embeds resume/JD into Chroma.
- [x] 2026-05-20 — Prompt 5 complete: added `/debug/retrieve` + `/debug/session/{session_id}`; retrieved 3+3 chunks and they clearly contained the query term (e.g., “FastAPI … SQLAlchemy … Postgres … ChromaDB …”).
- [x] 2026-05-29 — Phase 2 complete: added `agents/question_generator.py` (Gemini 2.0 Flash primary, Groq/LLaMA fallback), `routes/questions.py` (`POST /api/generate-questions`, `GET /api/questions/{session_id}`); added `question_type`, `difficulty`, `expected_themes` columns to Question model; wired router into main.py.
- [x] 2026-05-29 — Phase 3 complete: added `agents/evaluator.py` (Groq primary, Gemini fallback; 4-dim rubric), `agents/probe_generator.py` (Gemini primary; follow-up for weak answers), `routes/answers.py` (`POST /api/submit-answer`, `GET /api/answers/{session_id}`); probe circuit breaker via `is_probe=true` flag; wired answers router into main.py.
- [x] 2026-05-29 — Phase 4 complete: added `agents/summarizer.py` (Gemini primary, Groq fallback; aggregated report with strengths/weaknesses/resources/verdict), `routes/summary.py` (`POST /api/session/{id}/summarize`, `GET /api/session/{id}/summary`); probe answers excluded from scoring; wired summary router into main.py.
- [x] 2026-05-31 — Phase 6 complete: built full test suite in backend/tests/; pytest-asyncio + SQLite in-memory (StaticPool) + httpx AsyncClient; conftest.py fixtures for engine/db/client/test_session/test_question/test_answer; 4 route test files (upload, questions, answers, summary) + 4 agent unit test files; all LLM calls mocked at function level; renamed agent private functions for clean mock paths; changed ARRAY(String) → JSON in models for SQLite compat.
- [x] 2026-05-30 — Phase 5 complete: scaffolded Next.js 15 + Tailwind in /frontend; dark terminal aesthetic (DM Serif + JetBrains Mono + lime accent); 3 pages: upload/landing, interview flow (question card + eval panel + probe card + progress dots), final report (scores, strengths, weaknesses+actions, resources); lib/api.ts wires all FastAPI endpoints; fixed inst.md bugs (upload field names resume_file/jd_file, correct /api prefix routing, broken <a> tag in report).
